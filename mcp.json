{
  "mcpServers": {
    "ci-self-healer": {
      "command": "rote",
      "args": [
        "play",
        "run",
        "https://play.modiqo.ai/swapankumar/ci-self-healer",
        "target_dir=.",
        "provider=gemini",
        "model=gemini-3.5-flash",
        "push_strategy=direct",
        "auto_push=true",
        "--yes"
      ],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      },
      "description": "Autonomous Rote CI/CD Self-Healing Healer that auto-diagnoses build, lint, or test failures across Node.js, Python, Go, and Rust, applies line-edit repairs, verifies test suites, and pushes fixes."
    }
  }
}
